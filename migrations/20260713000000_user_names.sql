-- Migration placeholder: first_name and last_name columns already added
-- to the users table via manual ALTER during development.
-- In production, these columns would be added here:
--   ALTER TABLE users ADD COLUMN first_name TEXT NOT NULL DEFAULT '';
--   ALTER TABLE users ADD COLUMN last_name TEXT NOT NULL DEFAULT '';
SELECT 1;
